{
  "name": "set-gateway",
  "version": "2.1.0",
  "description": "SET — LLM gateway: OpenAI-compatible proxy with per-space usage metering and spend caps (PLAN.md Phase 5)",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fastify": "^4.26.2",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/pg": "^8.23.1",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  }
}
